defmodule Modules do
    @common_num 10

    def add_to(num) do
        num + @common_num
    end

    def sub_to(num) do
        num - @common_num
    end

    def mul_to(num) do
        num * @common_num
    end

    def mul_to(num) do
        num / @common_num
    end

end